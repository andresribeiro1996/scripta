# Audit — 2026-08-30

Scope: dead code, bloat, improvements. Method: cross-referenced every export and file against imports repo-wide, checked deps against usage, read key paths. Verdict up front: **the codebase is clean** — one dead file, no unused deps, no unused exports, no `any`/`console.log` noise, all routes reachable. The items below are the honest remainder.

## Dead code (verified, safe to delete)

1. `backend/src/modules/murals/domain/errors.ts` — comments-only placeholder, zero imports. Self-described as "reserved for Task 4" (share-token rules that shipped differently). Classic speculative file.

## Stale artifacts (decision needed)

2. `viewer/` (1194 lines) — README calls it superseded by `frontend/`; untouched since initial commit. Keep only if the standalone no-backend use case matters to you; otherwise archive to a branch and delete.
3. `design/card-redesign/` — exploration mockups, served their purpose.
4. `.claude/worktrees/` — empty leftover.

## Gaps

5. **Backend test script hardcodes one file** — `"test": "tsx --test src/modules/arena/service.test.ts"` silently ignores any new `*.test.ts`. Fix: `tsx --test src/modules/**/*.test.ts`. Cheap, prevents future surprise.
6. **Test coverage: 1 of 7 modules.** Only `arena` is tested. Riskiest untested: `auth` (refresh rotation, revocation), `library/migration.ts` + `runStartupMigrations` (data-moving on boot), `murals` block refs.
7. **Frontend: no tests at all**, no test script even defined.
8. **No CI** — nothing runs `typecheck`/`lint`/`test` on push. A ~20-line GitHub Actions workflow would lock in all of the above.

## Observations (not problems)

- 7 near-identical `adapters/sqlite/connection.ts` files — intentional module isolation, documented in each. Leave it.
- JWT handling, rate limiting (all 7 modules), zod validation, CORS scoping — all present and correct.
- `GroupsPage` looks unrouted but is imported by `SeriesPage`/`CollectionsPage` as a shared base. Alive.

## Suggested order

1. Delete item 1; decide on 2–4 → 5 min
2. Fix test glob (item 5) → 1 line
3. Add CI (item 8) → forces items 6–7 to matter
4. Backfill tests: auth first (item 6)
