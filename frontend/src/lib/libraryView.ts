// Moved to packages/shared/src/library/libraryView.ts (Task 3A). See
// lib/merge.ts's own comment for why this is a thin re-export.
export type { LibraryBook, StatusFilter, SortKey } from "@scripta/shared";
export { STATUS_FILTER_OPTIONS, SORT_OPTIONS, filterBooks, sortBooks, nextReadStatus } from "@scripta/shared";
