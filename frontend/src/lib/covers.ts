// Moved to packages/shared/src/library/covers.ts (Task 3A). See
// lib/merge.ts's own comment for why this is a thin re-export.
export { normalizeIsbn, normalizeImageId, statusLabel } from "@scripta/shared";
